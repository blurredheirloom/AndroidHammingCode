<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="bottom"
    tools:context="com.akropon.hammingcode.ResultActivity">


    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_above="@+id/btn_copy_all"
        android:layout_alignParentTop="true"
        android:background="@android:drawable/edit_text"
        android:scrollbars="none">

        <HorizontalScrollView
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:scrollbars="none">

            <TextView
                android:id="@+id/textview_result"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:fontFamily="monospace"
                android:text="aaaaaaaaaaaaa\nlllllllllllll\nX            X"
                android:textColor="@android:color/black" />
        </HorizontalScrollView>
    </ScrollView>


    <Button
        android:id="@+id/btn_copy_all"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_above="@+id/btn_copy_result_word"
        android:layout_centerHorizontal="true"
        android:textSize="16sp"
        android:text="Копировать все" />

    <Button
        android:id="@+id/btn_copy_result_word"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_above="@+id/btn_back"
        android:layout_centerHorizontal="true"
        android:textSize="16sp"
        android:text="Копировать выходное слово" />

    <Button
        android:id="@+id/btn_back"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:textSize="16sp"
        android:text="Назад" />

</RelativeLayout>
